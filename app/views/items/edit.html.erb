<%= provide(:title, "Editing #{@item.name}") %>

<%= render 'form' %>

<div class="row row-margin">
	<%= link_to link_icon('arrow-left', 'Back'), items_path, 
												class: btn_show('left') %>
	<%= link_to link_icon('gift', 'Show'), @item, 
												class: btn_show('right') %>
</div>