<nav class="navbar navbar-default" >
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" 
							data-toggle="collapse" data-target="#navbar" 
							aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if !user_signed_in? %>
      <% end %>
      <strong>
        <%= link_to 'Store', root_path, class: "navbar-brand" %>
      </strong>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <!-- <li>
          <% #if user_signed_in? %>
            <% #link_to link_icon('home', 'Home'), authenticated_root_path %>
          <% #end %>
      	</li> -->
      </ul>
      <ul class="nav navbar-nav navbar-right">
				<% if user_signed_in? %>
					<li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" 
                      role="button" aria-haspopup="true" aria-expanded="false">
    						<strong>
    							<%= link_icon('user', current_user.name) %> 
    							<span class="caret"></span>
  							</strong>
						</a>
            <ul class="dropdown-menu">
              <li>
              	<%= link_to "Profile", current_user %>
            	</li>
              <% if (can? :manage, User) || (can? :manage, Role) %>
                <li class="dropdown-header">Management</li>
              <% end %>
              <% if can? :manage, User %>
                <li><%= link_to "Users", users_path %></li>
              <% end %>
              <% if can? :manage, Role %>
              	<li><%= link_to "Roles", roles_path %></li>
              <% end %>
							<li role="separator" class="divider"></li>
							<li>
						  	<%= link_to link_icon('log-out', "Sign out"), destroy_user_session_path, 
			  																							:method => :delete %>
						  </li>
            </ul>
          </li>
				<% else %>
				  <!-- <li>
				  	<% # link_to "Sign up", new_user_registration_path %>
				  </li>
				 	<li>
				 		<% #link_to "Sign in", new_user_session_path %>
				 	</li>  -->
          <%= render 'devise/sessions/panel' %>
<!--           <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" 
                        role="button" aria-haspopup="true" 
                        aria-expanded="false">
                <strong>
                  Sign In
                  <span class="caret"></span>
                </strong>
            </a>
            <ul class="dropdown-menu" style="width: 400px;">
              <li>
                <% #render 'devise/sessions/login' %>
              </li>
            </ul>
          </li> -->
				<% end %>            
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>	
