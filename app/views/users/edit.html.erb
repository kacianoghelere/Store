<%= provide(:title, "Editing #{@user.name}") %>
<%= render 'form' %>

<div class="row row-margin">
	<%= link_to link_icon('arrow-left', 'Back'), users_path, 
																								class: btn_show('left') %>
	<%= link_to link_icon('user', 'Show'), @user, class: btn_show('right') %>
</div>