<%= provide(:title, "Editing #{@role.name}") %>
<%= render 'form' %>

<div class="row row-margin">
	<%= link_to link_icon('arrow-left', 'Back'), roles_path, 
																								class: btn_show('left') %>
	<%= link_to link_icon('user', 'Show'), @role, class: btn_show('right') %>
</div>
